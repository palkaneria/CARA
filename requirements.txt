# Redrob Ranker — Pinned Python Dependencies
# Install with: pip install -r requirements.txt

# Core scientific stack
numpy==2.4.6

# Embeddings (offline pre-computation only)
sentence-transformers==5.5.1
torch==2.12.0

# Vector search
faiss-cpu==1.14.3
